'use client';

import { SwapPanel } from './index';

const MobileSwapPage = () => (
  <main className="mx-auto min-h-[calc(100dvh-58px)] w-full px-4 pb-24 md:hidden">
    <SwapPanel />
  </main>
);

export default MobileSwapPage;
